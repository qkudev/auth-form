type TMaybe<T> = T | undefined;
type Nullable<T> = T | null;

/**
 * Valid email string
 */
type Email = string;

/**
 * Valid password string. At least 8 chars and should have digit
 */
type Password = string;
